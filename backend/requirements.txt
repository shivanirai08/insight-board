# Checkpoint 1 — FastAPI skeleton only.
# Later checkpoints add: auth, DB, pandas, dash, etc.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
httpx>=0.27.0
